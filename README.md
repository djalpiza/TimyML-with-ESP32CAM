# TimyML-with-ESP32CAM
The repository is for a TinyML project that makes use of the ESP32-CAM and neural network development. The project's objective is to create a compact, low-power gadget that can run a neural network model for image classification that has already been trained.
